/* ===== General ===== */
body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
  line-height: 1.6;
}

/* ===== Header ===== */
header {
  background: white; /* White background for your name */
  text-align: center;
  padding: 2rem 0;
  border-bottom: 2px solid black;
  overflow: hidden;
}

/* Dancing name */
.dancing-text span {
  display: inline-block;
  font-size: 2.5rem;
  font-weight: bold;
  animation: dance 1.5s infinite;
}
.dancing-text span:nth-child(odd) { animation-delay: 0.1s; }
.dancing-text span:nth-child(even) { animation-delay: 0.3s; }

@keyframes dance {
  0%   { transform: translateY(0); color: red; }
  25%  { transform: translateY(-10px); color: green; }
  50%  { transform: translateY(0); color: red; }
  75%  { transform: translateY(10px); color: green; }
  100% { transform: translateY(0); color: red; }
}

/* Train effect */
.train-text {
  display: inline-block;
  font-size: 1.5rem;
  font-weight: bold;
  color: darkred;
  white-space: nowrap;
  animation: trainMove 8s linear infinite;
}
@keyframes trainMove {
  0% { transform: translateX(-100%); }
  100% { transform: translateX(100%); }
}

/* ===== Navigation ===== */
nav {
  display: flex;
  justify-content: center;
  background: #333;
}
nav a {
  color: white;
  padding: 14px 20px;
  text-decoration: none;
}
nav a:hover { background: #575757; }

/* ===== Sections ===== */
section {
  padding: 20px;
  max-width: 800px;
  margin: 20px auto;
  border: 3px solid black;
  background: skyblue;
  border-radius: 50px;
}
section h2 {
  color: maroon; /* headings in maroon */
  text-align: center;
}
section p, .project p {
  color: black;
  font-family: "Georgia", serif;
}

/* Projects */
.project {
  background: white;
  padding: 15px;
  margin: 10px 0;
  border-radius: 10px;
  border: 1px solid #ccc;
}

/* Contact form */
form {
  display: flex;
  flex-direction: column;
  gap: 10px;
  align-items: center;
}
input, textarea, button {
  padding: 10px;
  font-size: 1rem;
  border-radius: 5px;
  border: 1px solid #ccc;
  width: 80%;
  max-width: 400px;
}
button {
  background: #4CAF50;
  color: white;
  border: none;
  cursor: pointer;
}
button:hover { background: #45a049; }

/* Footer */
footer {
  text-align: center;
  padding: 10px;
  background: #333;
  color: white;
  margin-top: 20px;
}
